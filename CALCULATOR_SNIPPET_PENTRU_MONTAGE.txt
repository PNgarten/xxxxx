═══════════════════════════════════════════════════════════════════
CALCULATOR SNIPPET PENTRU MONTAGE.HTML
═══════════════════════════════════════════════════════════════════

INSTRUCȚIUNI PAUL:

1. Deschide fișierul montage.html din website
2. Găsește tagul </style> (sfârșitul stilurilor CSS)
3. ÎNAINTE de </style>, adaugă CSS-UL DE MAI JOS
4. Găsește secțiunea cu FAQ sau unde vrei calculatorul
5. Adaugă HTML-UL calculatorului acolo
6. La SFÂRȘITUL fișierului, ÎNAINTE de </body>, adaugă JAVASCRIPT-ul

═══════════════════════════════════════════════════════════════════
PASUL 1: ADAUGĂ CSS (înainte de </style>)
═══════════════════════════════════════════════════════════════════

        /* ==== CALCULATOR STYLES - START ==== */
        .calculator-wrapper { 
            background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%); 
            padding: 5rem 3rem; 
        }
        
        .calculator-container { 
            max-width: 1200px; 
            margin: 0 auto; 
            background: #fff; 
            border-radius: 20px; 
            box-shadow: 0 10px 40px rgba(0,0,0,0.1); 
            overflow: hidden; 
        }
        
        .calculator-header { 
            background: linear-gradient(135deg, var(--purple) 0%, #5B21B6 100%); 
            color: #fff; 
            padding: 3rem; 
            text-align: center; 
        }
        
        .calculator-header h2 { 
            font-size: 2.5rem; 
            font-weight: 900; 
            margin-bottom: 1rem; 
        }
        
        .calculator-header p { 
            font-size: 1.1rem; 
            opacity: 0.95; 
        }
        
        .calculator-content { 
            display: grid; 
            grid-template-columns: 2fr 1fr; 
            gap: 2rem; 
            padding: 2rem; 
        }
        
        .calculator-inputs { 
            padding-right: 2rem; 
            max-height: 70vh; 
            overflow-y: auto; 
        }
        
        .calc-section { 
            margin-bottom: 2rem; 
            padding: 1.5rem; 
            background: var(--light); 
            border-radius: 12px; 
            border-left: 4px solid var(--purple); 
        }
        
        .calc-section-title { 
            font-size: 1.3rem; 
            font-weight: 700; 
            color: var(--purple); 
            margin-bottom: 1rem; 
            display: flex; 
            align-items: center; 
            gap: 0.5rem; 
        }
        
        .input-group { 
            margin-bottom: 1rem; 
        }
        
        .input-group label { 
            display: block; 
            font-weight: 600; 
            margin-bottom: 0.5rem; 
            color: var(--gray); 
        }
        
        .input-group select, 
        .input-group input[type="number"] { 
            width: 100%; 
            padding: 0.8rem; 
            border: 2px solid #ddd; 
            border-radius: 8px; 
            font-size: 1rem; 
            transition: all 0.3s; 
        }
        
        .input-group select:focus, 
        .input-group input:focus { 
            outline: none; 
            border-color: var(--purple); 
            box-shadow: 0 0 0 3px rgba(124,58,237,0.1); 
        }
        
        .input-group small {
            display: block;
            margin-top: 0.3rem;
            color: #666;
            font-size: 0.85rem;
        }
        
        .checkbox-group { 
            display: flex; 
            align-items: center; 
            gap: 0.8rem; 
            padding: 0.8rem; 
            background: #fff; 
            border-radius: 8px; 
            margin-bottom: 0.5rem; 
        }
        
        .checkbox-group input[type="checkbox"] { 
            width: 20px; 
            height: 20px; 
            cursor: pointer; 
        }
        
        .checkbox-group label { 
            margin: 0; 
            cursor: pointer; 
            font-weight: 500; 
        }
        
        .result-panel { 
            position: sticky; 
            top: 2rem; 
            background: linear-gradient(135deg, #fff 0%, var(--light) 100%); 
            padding: 2rem; 
            border-radius: 15px; 
            box-shadow: 0 4px 20px rgba(0,0,0,0.08); 
        }
        
        .result-panel h3 { 
            font-size: 1.5rem; 
            font-weight: 700; 
            color: var(--purple); 
            margin-bottom: 1.5rem; 
            text-align: center; 
        }
        
        .result-content { 
            min-height: 200px; 
        }
        
        .result-item { 
            display: flex; 
            justify-content: space-between; 
            padding: 0.8rem 0; 
            border-bottom: 1px solid #eee; 
        }
        
        .result-item:last-child { 
            border-bottom: none; 
        }
        
        .result-total { 
            font-size: 1.8rem; 
            font-weight: 900; 
            color: var(--purple); 
        }
        
        .result-total-section { 
            margin-top: 1.5rem; 
            padding-top: 1.5rem; 
            border-top: 2px solid var(--purple); 
        }
        
        .calc-buttons { 
            display: grid; 
            grid-template-columns: 1fr 1fr; 
            gap: 1rem; 
            margin-top: 1.5rem; 
        }
        
        .btn-calculate { 
            background: linear-gradient(135deg, var(--purple) 0%, #5B21B6 100%); 
            color: #fff; 
            padding: 1rem; 
            border: none; 
            border-radius: 10px; 
            font-size: 1.1rem; 
            font-weight: 700; 
            cursor: pointer; 
            transition: all 0.3s; 
        }
        
        .btn-calculate:hover { 
            transform: translateY(-2px); 
            box-shadow: 0 8px 20px rgba(124,58,237,0.3); 
        }
        
        .btn-reset { 
            background: #fff; 
            color: var(--purple); 
            border: 2px solid var(--purple); 
            padding: 1rem; 
            border-radius: 10px; 
            font-size: 1.1rem; 
            font-weight: 700; 
            cursor: pointer; 
            transition: all 0.3s; 
        }
        
        .btn-reset:hover { 
            background: var(--purple); 
            color: #fff; 
        }
        
        @media (max-width: 1024px) { 
            .calculator-content { 
                grid-template-columns: 1fr; 
            }
            .result-panel { 
                position: static; 
            }
        }
        
        @media (max-width: 768px) { 
            .calculator-inputs { 
                max-height: none; 
            }
            .calculator-header h2 {
                font-size: 1.8rem;
            }
        }
        /* ==== CALCULATOR STYLES - END ==== */


═══════════════════════════════════════════════════════════════════
PASUL 2: ADAUGĂ HTML (unde vrei să apară calculatorul în pagină)
═══════════════════════════════════════════════════════════════════

    <!-- CALCULATOR SECTION - START -->
    <div class="calculator-wrapper">
        <div class="calculator-container">
            <div class="calculator-header">
                <h2>🧮 Kostenrechner Möbelmontage</h2>
                <p>Berechnen Sie sofort Ihre Montagekosten – transparent und unverbindlich</p>
            </div>
            
            <div class="calculator-content">
                <div class="calculator-inputs">
                    <!-- IKEA Möbel -->
                    <div class="calc-section">
                        <div class="calc-section-title">🛋️ IKEA Möbel</div>
                        <div class="input-group">
                            <label>Kleiderschränke (PAX, BRIMNES, etc.)</label>
                            <select id="ikea-wardrobe">
                                <option value="0-0">Keine Auswahl</option>
                                <option value="80-150">Kleiner Schrank (2 Türen, <120cm): €80-150</option>
                                <option value="150-300">Mittlerer Schrank (3 Türen, 120-200cm): €150-300</option>
                                <option value="300-550">Großer Schrank (4+ Türen, >200cm): €300-550</option>
                            </select>
                        </div>
                        <div class="input-group">
                            <label>Bettgestelle (MALM, HEMNES, etc.)</label>
                            <select id="ikea-bed">
                                <option value="0-0">Keine Auswahl</option>
                                <option value="60-100">Einfaches Bett (90-140cm): €60-100</option>
                                <option value="80-140">Doppelbett (160-180cm): €80-140</option>
                                <option value="120-200">Bett mit Stauraum: €120-200</option>
                            </select>
                        </div>
                        <div class="input-group">
                            <label>Regale & Kommoden</label>
                            <select id="ikea-storage">
                                <option value="0-0">Keine Auswahl</option>
                                <option value="40-80">Kleine Kommode (<100cm): €40-80</option>
                                <option value="60-120">Großes Regal (BILLY, KALLAX): €60-120</option>
                                <option value="100-200">Komplexes System: €100-200</option>
                            </select>
                        </div>
                        <div class="input-group">
                            <label>Anzahl weiterer kleiner IKEA Möbel</label>
                            <input type="number" id="ikea-small" min="0" max="20" value="0" placeholder="0">
                            <small>Nachttische, Stühle, kleine Tische: €30-70/Stück</small>
                        </div>
                    </div>

                    <!-- Küche -->
                    <div class="calc-section">
                        <div class="calc-section-title">🏠 Küchenmontage</div>
                        <div class="input-group">
                            <label>Küchenzeile</label>
                            <select id="kitchen-line">
                                <option value="0-0">Keine Auswahl</option>
                                <option value="800-1500">Kleine Küche (<4 Meter): €800-1.500</option>
                                <option value="1500-2500">Mittlere Küche (4-6 Meter): €1.500-2.500</option>
                                <option value="2500-4000">Große L-Küche (6-10 Meter): €2.500-4.000</option>
                                <option value="4000-6000">Komplexe U-Küche (>10 Meter): €4.000-6.000</option>
                            </select>
                        </div>
                        <div class="checkbox-group">
                            <input type="checkbox" id="kitchen-sink">
                            <label for="kitchen-sink">Spüle + Wasserhahn montieren (+€80-150)</label>
                        </div>
                        <div class="checkbox-group">
                            <input type="checkbox" id="kitchen-appliances">
                            <label for="kitchen-appliances">Geräte anschließen (Herd, Spüler) (+€150-300)</label>
                        </div>
                        <div class="checkbox-group">
                            <input type="checkbox" id="kitchen-wall">
                            <label for="kitchen-wall">Wandschrank-Montage (+€200-400)</label>
                        </div>
                    </div>

                    <!-- Andere Möbel -->
                    <div class="calc-section">
                        <div class="calc-section-title">🪑 Andere Möbel</div>
                        <div class="input-group">
                            <label>Sonstiges</label>
                            <select id="other-furniture">
                                <option value="0-0">Keine Auswahl</option>
                                <option value="150-300">Büromöbel-Set: €150-300</option>
                                <option value="120-250">Wohnwand: €120-250</option>
                                <option value="80-150">Esszimmergarnitur: €80-150</option>
                                <option value="100-200">Badmöbel-Set: €100-200</option>
                            </select>
                        </div>
                    </div>

                    <!-- Carports -->
                    <div class="calc-section">
                        <div class="calc-section-title">🚗 Carports & Außen</div>
                        <div class="input-group">
                            <label>Carport/Pavillon</label>
                            <select id="outdoor">
                                <option value="0-0">Keine Auswahl</option>
                                <option value="600-1200">Einfacher Carport (1 Auto): €600-1.200</option>
                                <option value="1200-2000">Doppel-Carport: €1.200-2.000</option>
                                <option value="500-1000">Pavillon (3×3m): €500-1.000</option>
                                <option value="1000-2000">Großer Pavillon (4×4m+): €1.000-2.000</option>
                            </select>
                        </div>
                        <div class="input-group">
                            <label>Gartenmöbel-Sets</label>
                            <input type="number" id="garden-sets" min="0" max="10" value="0" placeholder="0">
                            <small>Loungesets, Essgruppen: €80-200/Set</small>
                        </div>
                    </div>

                    <!-- Zusatzleistungen -->
                    <div class="calc-section">
                        <div class="calc-section-title">➕ Zusatzleistungen</div>
                        <div class="checkbox-group">
                            <input type="checkbox" id="wall-mounting">
                            <label for="wall-mounting">Wandmontage (TV, Regale, Spiegel) (+€50-120)</label>
                        </div>
                        <div class="input-group">
                            <label>Kippsicherungen (Schränke)</label>
                            <input type="number" id="antitip-count" min="0" max="20" value="0" placeholder="0">
                            <small>€15-40/Stück je nach Größe</small>
                        </div>
                        <div class="checkbox-group">
                            <input type="checkbox" id="packaging">
                            <label for="packaging">Verpackung entsorgen (+€30-80)</label>
                        </div>
                        <div class="checkbox-group">
                            <input type="checkbox" id="old-furniture">
                            <label for="old-furniture">Alte Möbel abbauen (+€100-300)</label>
                        </div>
                    </div>
                </div>

                <!-- RESULT PANEL -->
                <div class="result-panel">
                    <h3>💰 Ihre Kosten</h3>
                    <div id="result-content" class="result-content">
                        <p style="text-align:center; opacity:0.7; padding:20px;">Wählen Sie Möbel aus, um eine Berechnung zu erhalten</p>
                    </div>
                    <div class="calc-buttons">
                        <button class="btn-calculate" onclick="calculateTotal()">Berechnen</button>
                        <button class="btn-reset" onclick="resetCalculator()">Zurücksetzen</button>
                    </div>
                    <p style="text-align:center; margin-top:1.5rem; font-size:0.85rem; color:#666;">
                        <strong>Hinweis:</strong> Dies ist eine unverbindliche Schätzung. Endpreis nach Besichtigung.
                    </p>
                </div>
            </div>
        </div>
    </div>
    <!-- CALCULATOR SECTION - END -->


═══════════════════════════════════════════════════════════════════
PASUL 3: ADAUGĂ JAVASCRIPT (la final, înainte de </body>)
═══════════════════════════════════════════════════════════════════

    <script>
        function calculateTotal() {
            let total = { min: 0, max: 0 };
            let items = [];

            function parsePrice(priceStr) {
                const parts = priceStr.split('-');
                return { min: parseInt(parts[0]), max: parseInt(parts[1]) };
            }

            // IKEA Möbel
            const ikeaWardrobe = document.getElementById('ikea-wardrobe').value;
            if (ikeaWardrobe !== '0-0') {
                const p = parsePrice(ikeaWardrobe);
                total.min += p.min; total.max += p.max;
                items.push(`IKEA Kleiderschrank: €${p.min}-€${p.max}`);
            }

            const ikeaBed = document.getElementById('ikea-bed').value;
            if (ikeaBed !== '0-0') {
                const p = parsePrice(ikeaBed);
                total.min += p.min; total.max += p.max;
                items.push(`IKEA Bett: €${p.min}-€${p.max}`);
            }

            const ikeaStorage = document.getElementById('ikea-storage').value;
            if (ikeaStorage !== '0-0') {
                const p = parsePrice(ikeaStorage);
                total.min += p.min; total.max += p.max;
                items.push(`IKEA Regal/Kommode: €${p.min}-€${p.max}`);
            }

            const ikeaSmall = parseInt(document.getElementById('ikea-small').value) || 0;
            if (ikeaSmall > 0) {
                total.min += ikeaSmall * 30; total.max += ikeaSmall * 70;
                items.push(`${ikeaSmall}× Kleine IKEA Möbel: €${ikeaSmall * 30}-€${ikeaSmall * 70}`);
            }

            // Küche
            const kitchen = document.getElementById('kitchen-line').value;
            if (kitchen !== '0-0') {
                const p = parsePrice(kitchen);
                total.min += p.min; total.max += p.max;
                items.push(`Küchenzeile: €${p.min}-€${p.max}`);

                if (document.getElementById('kitchen-sink').checked) {
                    total.min += 80; total.max += 150;
                    items.push(`Spüle + Wasserhahn: €80-€150`);
                }
                if (document.getElementById('kitchen-appliances').checked) {
                    total.min += 150; total.max += 300;
                    items.push(`Geräte anschließen: €150-€300`);
                }
                if (document.getElementById('kitchen-wall').checked) {
                    total.min += 200; total.max += 400;
                    items.push(`Wandschränke: €200-€400`);
                }
            }

            // Andere Möbel
            const otherFurniture = document.getElementById('other-furniture').value;
            if (otherFurniture !== '0-0') {
                const p = parsePrice(otherFurniture);
                total.min += p.min; total.max += p.max;
                items.push(`Sonstige Möbel: €${p.min}-€${p.max}`);
            }

            // Outdoor
            const outdoor = document.getElementById('outdoor').value;
            if (outdoor !== '0-0') {
                const p = parsePrice(outdoor);
                total.min += p.min; total.max += p.max;
                items.push(`Carport/Pavillon: €${p.min}-€${p.max}`);
            }

            const gardenSets = parseInt(document.getElementById('garden-sets').value) || 0;
            if (gardenSets > 0) {
                total.min += gardenSets * 80; total.max += gardenSets * 200;
                items.push(`${gardenSets}× Gartenmöbel-Set: €${gardenSets * 80}-€${gardenSets * 200}`);
            }

            // Zusatzleistungen
            if (document.getElementById('wall-mounting').checked) {
                total.min += 50; total.max += 120;
                items.push(`Wandmontage: €50-€120`);
            }

            const antitip = parseInt(document.getElementById('antitip-count').value) || 0;
            if (antitip > 0) {
                total.min += antitip * 15; total.max += antitip * 40;
                items.push(`${antitip}× Kippsicherung: €${antitip * 15}-€${antitip * 40}`);
            }

            if (document.getElementById('packaging').checked) {
                total.min += 30; total.max += 80;
                items.push(`Verpackung entsorgen: €30-€80`);
            }

            if (document.getElementById('old-furniture').checked) {
                total.min += 100; total.max += 300;
                items.push(`Alte Möbel abbauen: €100-€300`);
            }

            // Ergebnis anzeigen
            if (total.min === 0 && total.max === 0) {
                alert('Bitte wählen Sie mindestens ein Möbelstück aus!');
                return;
            }

            const nettoMin = Math.round(total.min / 1.19);
            const nettoMax = Math.round(total.max / 1.19);
            const mwstMin = total.min - nettoMin;
            const mwstMax = total.max - nettoMax;

            let resultHTML = '<div style="max-height: 300px; overflow-y: auto; margin-bottom: 15px;">';
            items.forEach(item => {
                resultHTML += `<div class="result-item"><span style="font-size:0.9rem;">${item}</span></div>`;
            });
            resultHTML += '</div>';

            resultHTML += '<div class="result-total-section">';
            resultHTML += `<div class="result-item"><span>Netto:</span><span>€${nettoMin} - €${nettoMax}</span></div>`;
            resultHTML += `<div class="result-item"><span>MwSt (19%):</span><span>€${mwstMin} - €${mwstMax}</span></div>`;
            resultHTML += `<div class="result-item" style="padding-top:15px;"><span><strong>GESAMT:</strong></span><span class="result-total">€${total.min} - €${total.max}</span></div>`;
            resultHTML += '</div>';

            document.getElementById('result-content').innerHTML = resultHTML;
            document.getElementById('result-content').scrollIntoView({ behavior: 'smooth', block: 'nearest' });
        }

        function resetCalculator() {
            document.querySelectorAll('select').forEach(s => s.value = '0-0');
            document.querySelectorAll('input[type="checkbox"]').forEach(cb => cb.checked = false);
            document.querySelectorAll('input[type="number"]').forEach(input => input.value = 0);
            document.getElementById('result-content').innerHTML = '<p style="text-align:center; opacity:0.7; padding:20px;">Wählen Sie Möbel aus, um eine Berechnung zu erhalten</p>';
        }
    </script>


═══════════════════════════════════════════════════════════════════
UNDE SĂ PUI HTML-ul ÎN PAGINA MONTAGE.HTML
═══════════════════════════════════════════════════════════════════

Găsește în montage.html:

    </section>  <!-- Sfârșitul secțiunii "hero-service" -->

Și IMEDIAT DUPĂ aceasta, adaugă toată secțiunea calculator (HTML de mai sus).

SAU

Găsește secțiunea cu prețuri și adaugă calculatorul ÎNAINTE de ea.

═══════════════════════════════════════════════════════════════════
CHECKLIST FINAL
═══════════════════════════════════════════════════════════════════

□ CSS adăugat ÎNAINTE de </style>
□ HTML calculator adăugat unde vrei în pagină
□ JavaScript adăugat ÎNAINTE de </body>
□ Salvat fișierul
□ Upload pe server
□ Testat în browser

═══════════════════════════════════════════════════════════════════
DACĂ TOT NU MERGE
═══════════════════════════════════════════════════════════════════

Trimite-mi:
1. Screenshot cu ce vezi pe website
2. Screenshot console browser (F12 → Console)
3. Sau dă-mi acces FTP și îl fac eu direct!

═══════════════════════════════════════════════════════════════════
